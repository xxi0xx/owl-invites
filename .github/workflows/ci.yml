name: CI

on:
  push:
    branches: [main, 'codex/**']
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  product-verification:
    name: Authoritative product gate
    uses: ./.github/workflows/verify.yml
    permissions:
      contents: read
